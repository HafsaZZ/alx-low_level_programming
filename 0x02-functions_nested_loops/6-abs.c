#include "main.h"
/**
 * _abs - computes the absolute value for an interger
 * @n: interger to be tested
 * Return: 0 or 1
 */

int _abs(int n)

{
	if(i>=0){
		return(i);
	}
	else{
		return(i*-1);
	}
}
